<%= form_with(model: tax_rate, local: true) do |form| %>
  <% if tax_rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tax_rate.errors.count, "error") %> prohibited this tax rate from being saved:</h2>

      <ul>
        <% tax_rate.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :rate %>
    <%= form.number_field :rate, step: 0.0001 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
